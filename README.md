# a full stack app react at front end and nodejs at backend.
used postgresql for data persistence.
used docker to build image and docker compose to containerise the application.
used nginx proxy for routing.
